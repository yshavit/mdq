name: Release assets
on:
  pull_request:
    types: [ "opened", "synchronize" ]
    branches: [ "main" ]
    paths:
      - Cargo.toml

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "ref is ${{ github.head_ref }}"

  upload:
    if: startsWith(github.head_ref, 'pending-releases/')
    runs-on: ubuntu-latest
    steps:
      - run: echo "The head of this PR starts with 'pending-releases/'. TODO!"


